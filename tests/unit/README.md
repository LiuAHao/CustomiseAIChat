# tests/unit

这里预留给跨包单元测试和未来需要独立维护的逻辑测试。

当前阶段主单元测试仍放在源码包内，例如：

- `internal/app/app_test.go`

原因：

- 包内测试离业务最近
- 可直接访问当前包的非导出逻辑
- 适合快速验证应用层主链路

当后续出现以下情况时，可以把测试逐步放到这里：

- 需要从调用方视角测试导出接口
- 需要跨多个包组织测试数据
- 需要单独管理测试辅助方法
